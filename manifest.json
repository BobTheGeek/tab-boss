{
  "manifest_version": 3,
  "name": "Tab Boss (cloning OFF)",
  "version": "1.2.1",
  "description": "New tabs to the bottom. Snapshots on. Auto-clone DISABLED: a new ego lite Space is indistinguishable from Cmd+N (tb-l56).",
  "permissions": [
    "tabs",
    "tabGroups",
    "storage",
    "alarms"
  ],
  "action": {
    "default_title": "Tab Boss — restore the newest tab layout"
  },
  "background": {
    "service_worker": "src/background.js",
    "type": "module"
  }
}
